/** @type {import('next').NextConfig} */
const nextConfig = {
    images : {
        domains : ['encrypted-tbn0.gstatic.com', 'img-b.udemycdn.com', 'chromaticdreamers.com','cdn.prod.website-files.com','images.unsplash.com']
    }
};

export default nextConfig;
